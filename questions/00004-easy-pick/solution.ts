// MyPick<T, K>는 T 타입에서 K에 해당하는 속성들만 선택해서 새로운 타입을 만드는 타입이다.
// [P in K]에서 in 키워드는 유니온 타입(K)의 요소들을 하나씩 순회하며 새로운 타입을 생성하는 역할을 한다.
// T[P]는 T 객체에서 P에 해당하는 속성의 타입을 조회한다.
// 즉, K의 각 요소에 대해 T의 해당 속성의 타입을 추출하여 새로운 객체 타입을 생성한다.

export type MyPick<T, K extends keyof T> = {
  [P in K]: T[P];
}
